# Build output. Regenerated by tools\build.ps1 and tools\preview.py.
dist/

# Machine-local tool settings, not part of the addon.
.claude/

# The docs/ directory is deliberately NOT ignored: GitHub Pages serves it, and
# that is where the TV fetches the addon and its updates from.

__pycache__/
*.py[cod]

# Local addon_data written when the test scripts run against the stubs.
.testprofile/
